# Accessibility

- Every Canvas destination exists as a DOM route and an accessible mirror list.
- The hub Canvas supports keyboard arrows and selection.
- Audio and device orientation require explicit activation.
- Reduced motion, high contrast and reduced effects are available.
- Critical text never exists only inside Canvas.
- Focus styles, skip link, semantic landmarks and form labels are included.
