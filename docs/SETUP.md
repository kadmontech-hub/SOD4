# Setup

1. Install Node.js 20+.
2. Copy `.env.example` if desired.
3. Run `npm start`.
4. Visit the printed URL.
5. Use `/admin` to edit the daily Hub content.

The server reads environment variables directly; no dotenv dependency is used. Export variables in the shell or configure them in the host.
