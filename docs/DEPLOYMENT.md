# Deployment

This version can run on any Node 20+ host with writable persistent storage. For ephemeral/serverless hosting, replace the JSON store with a managed database. Set `HOST=0.0.0.0` and a platform-provided `PORT`. Serve behind HTTPS for service worker installation and device orientation APIs outside localhost.
