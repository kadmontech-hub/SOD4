# Content model

Content entities currently live in `public/js/content.js` and are generated or mapped to stable IDs. The next migration should move editorial content to JSON/database records with versions, status, locale and media references. User state must stay separate from editorial content.
